# get_absorbing_states

```
ludics.get_absorbing_states(state_space)
```

Returns the absorbing states in the state space

### Parameters:

- `state_space` - _numpy.array_ - an array of possible states

### Returns:

- _numpy.array_ - array of all absorbing states in the state space.
  Returns _None_ if no absorbing states exist
