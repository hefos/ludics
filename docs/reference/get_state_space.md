# get_state_space

```
ludics.get_state_space(N, k)
```

Generates a state space

### Parameters:

- `N` : _int_ - the number of players in the population
- `k` : _int_ - the number of actions which players may take

### Returns:

- _numpy.array_ - an array of all possible states
