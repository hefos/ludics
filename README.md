# Ludics

A library for the study of heterogeneous population dynamics.

## Usage

To run tests:

```
$ uv run pytest tests
```

To run benchmarks:

```
$ uv run pytest benchmarks
```

To run ruff:

```
$ uv run ruff format
```

To check ruff:

```
$ uv run ruff check
```
